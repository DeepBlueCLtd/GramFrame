# Tasks: Spectrograph Player

**Input**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/](./contracts/), [quickstart.md](./quickstart.md)

**Tests**: REQUIRED — the constitution's Principle II and SC-004/SC-005/SC-007
name them. Each story phase ends with its Playwright spec; the signal chain is
Vitest-covered in Phase 2.

**Organization**: by user story. Story 1 is complete (research.md). Phase 2 is
the foundation every later story builds on.

## Format: `[ID] [P?] [Story] Description`

## Standing gate (every task that changes code)

```bash
yarn typecheck && yarn lint && yarn test:unit && yarn hygiene && yarn build && yarn test
```

---

## Phase 1: Story 1 — Research spike ✅

- [x] T001 [US1] Score candidates, run the empirical probes, decide: `research.md`, `spike/`

## Phase 2: Foundational — state, config, signal chain

- [ ] T002 [P] `src/types.js`: `PlayerState`, `AnalysisParams`, `ImageDetails.timeStretch`; `src/core/state.js`: `player` core slice; update `tests/unit/mode-registration.test.js` frozen keys
- [ ] T003 [P] `src/core/browserCompatibility.js`: `HTMLAudioElement` and `HTMLCanvasElement.prototype.toDataURL` in `REQUIRED_APIS` (D15)
- [ ] T004 `src/core/configuration.js`: detect the `<audio>` first row, parse the five analysis rows, warn on `time-*`, validate per data-model §3
- [ ] T005 [P] `src/audio/wavDecoder.js` + `tests/unit/wav-decoder.test.js` (8/16/24/32/float, stereo mix, every error)
- [ ] T006 [P] `src/audio/fft.js` + `tests/unit/fft.test.js` (impulse, single tone, Parseval, size guard)
- [ ] T007 `src/audio/spectrogram.js` + `tests/unit/spectrogram.test.js` (plan maths, Nyquist clamp, known tone → expected bin, sliced == sync)
- [ ] T008 [P] `src/audio/gramImage.js` + `tests/unit/gram-image.test.js` (levels normalisation, size cap message, LUT shape)
- [ ] T009 [P] `src/audio/audioSource.js` (fetch, sidecar fallback) and `scripts/wav2js.mjs`
- [ ] T010 [P] `tests/fixtures/audio/tone-20s.wav` (generated by `spike/gen-wav.mjs`, 20 s @ 8 kHz) and the sidecar for the fallback fixture

## Phase 3: Story 2 — an audio instance renders a (blank) gram

- [ ] T011 `src/player/audioSetup.js`: load → decode → plan → analyse (progress) → paint → set `imageDetails` (natural, render 900×400, `timeStretch`), `config`, `player.ready`; deferred restore + storage listener (D12); FR-007 error path via `gram-frame-image-error` + console
- [ ] T012 `src/components/svgLayout.js`: stretch-aware placement (contract geometry) and the playhead clip; `src/utils/coordinates.js`: shortcut guard
- [ ] T013 `src/main.js` / `UISetup.setupSpectrogramIfAvailable`: route audio instances to `audioSetup`; skip restore/storage listener until ready; `src/gramframe.css`: `gram-frame-analysing` caption with `attr(data-gram-progress)`, `gram-frame-audio`
- [ ] T014 `tests/fixtures/player-page.html`, `tests/fixtures/player-sidecar-page.html`; `GramFramePage.waitForPlayerReady()`
- [ ] T015 `tests/player-load.spec.js`: AS-2.1..2.6 — defaults, overrides in state, blank view with axes `[−window, 0]`, sidecar fallback, bad file → error indicator with a second healthy instance, unsupported browser warning, oversize → error

## Phase 4: Story 3 — play: the gram scrolls while the audio is heard

- [ ] T016 `src/player/transport.js`: the `<audio>` element, `PlayerController`, event mirroring, `instance.player`
- [ ] T017 `src/player/playerView.js`: `syncViewToPlayhead`, follow loop, `clampViewTop`, `setViewTop`, `isPlaying`, `isTimeRevealed`, visibility catch-up
- [ ] T018 `src/core/events.js`: inert-while-playing gate; `src/core/keyboardControl.js`: skip nudges while playing; `gramframe.css`: `gram-frame-playing` cursor
- [ ] T019 `tests/fixtures/player-two-page.html`; `tests/player-playback.spec.js`: AS-3.1..3.6 and SC-003 (top row within 100 ms of `currentTime`; ≥ 30 frames/s)

## Phase 5: Story 4 — pause, annotate, resume

- [ ] T020 `src/modes/BaseMode.js` `isTimeRevealed`; one guard each in `AnalysisMode`, `PinSetMode`, `DopplerMode`
- [ ] T021 `src/core/viewport.js`: player-aware `panByNormalized`, `zoomAtImagePoint`, `zoomIn/zoomOut`; `PanMode.resolvePanDrag` / `idleCursor` allow vertical pan at zoom 1 on audio instances
- [ ] T022 `tests/player-annotations.spec.js`: AS-4.1..4.6 and SC-005 (harmonic set stays on its tonal through a window of scrolling and a loop back to 0), reload persistence

## Phase 6: Story 5 — full transport

- [ ] T023 `src/components/TransportBar.js` + CSS; mounted by `audioSetup` when ready
- [ ] T024 `src/core/keyboardControl.js`: `Space`/`K`/`J`/`L`/`Home`/`M` on a focused audio instance; `src/core/events.js`: time-axis click seek
- [ ] T025 `src/api/GramFrameAPI.js` `getPlayer(index)`; `tests/public-api.spec.js` gains the audio-less case (`null`)
- [ ] T026 `tests/player-transport.spec.js`: AS-5.1..5.5, keyboard non-collision, autoplay refusal rejects

## Phase 7: Story 6 — material and documentation

- [ ] T027 [P] `sample/audio/*.wav` (done by the sourcing task: four CC BY 4.0 Work With Sounds recordings) + sidecars via `scripts/wav2js.mjs` + `ATTRIBUTION.md`
- [ ] T028 [P] `sample/player.html` (one player per file, debug-flagged), link from `debug.html`; `tests/smoke/` opens it
- [ ] T029 [P] `docs/HTML-Integration-Guide.md` audio section; `docs/ADRs/ADR-019-Audio-Sourced-Instances.md`; `CLAUDE.md` file list; `README.md` mention
- [ ] T030 `hygiene-baseline.json` — lower any count the work reduced; never raise one

## Dependencies

- Phase 2 before everything. T005–T008 are independent of each other; T007 depends on T006.
- Story 2 (T011–T015) before Stories 3–5. Story 3 before 4 and 5. Story 6 is independent once T009 exists.
